# Close Process

1. Re-read the issue goal and current trace.
2. Confirm the issue includes `Outcome` and `Evidence`.
3. Set `Outcome` to `success` or `failed`.
4. Add a short `Closure Note` describing why the loop is closed.
5. If you run the cheap probe, keep the `ready` / `not_ready` result visible instead of hiding failure.
