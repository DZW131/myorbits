# Triage Process

1. Create an issue from `docs/issues/templates/issue.md`.
2. Summarize the problem in one paragraph.
3. Record the current `Status`, keep `Outcome: pending`, and write the next action.
4. Link the first evidence artifact before moving the issue beyond `triaged`.
